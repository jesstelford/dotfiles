# This is the configuration file for Ghostty.
#
# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
#
# Additionally, each config option is also explained in detail
# on Ghostty's website, at https://ghostty.org/docs/config.

# Config syntax crash course
# ==========================
# # The config file consists of simple key-value pairs,
# # separated by equals signs.
# font-family = Iosevka
# window-padding-x = 2
#
# # Spacing around the equals sign does not matter.
# # All of these are identical:
# key=value
# key= value
# key =value
# key = value
#
# # Any line beginning with a # is a comment. It's not possible to put
# # a comment after a config option, since it would be interpreted as a
# # part of the value. For example, this will have a value of "#123abc":
# background = #123abc
#
# # Empty values are used to reset config keys to default.
# key =
#
# # Some config options have unique syntaxes for their value,
# # which is explained in the docs for that config option.
# # Just for example:
# resize-overlay-duration = 4s 200ms

# Arthur, one of Ghostty's built-in themes -- warm earth tones (tan, amber,
# sage) with cool cornflower/steel accents on #1c1c1c. Dark-only, so it
# satisfies by construction the constraint the previous NightOwlDark pinning
# enforced by hand: the terminal stays dark whatever macOS appearance is doing,
# same as Terminal.app's fixed "Clear Dark" profile. NightOwlDark/Light are
# still in ./themes/, so switching back is a one-liner.
#
# Neovim is coupled to this: ../nvim/colors/arthur.lua derives its highlight
# groups from Arthur's 16 ANSI colors so the editor and the terminal around it
# agree. Changing the theme here without regenerating that file desyncs them.
theme = Arthur

# Derive palette indices 16-255 from the theme's base 16 instead of leaving them
# at the fixed xterm cube, so 256-color apps sit in the same palette as
# everything else. Off by default upstream because legacy programs hardcode the
# xterm values; `harmonious` additionally reverses the ordering, which trips
# anything assuming 16-231 runs darkest to lightest. Drop both if some TUI
# suddenly renders unreadable. Any explicit `palette = N=#hex` still wins.
# Both require Ghostty >= 1.3.0.
palette-generate = true
palette-harmonious = true

# Setting to empty string resets the value, otherwise we're just appending fallbacks
font-family = ""
# The Nerd Font variant, not plain "Input Mono": powerlevel10k runs in
# nerdfont-complete mode (see .p10k.zsh) so the prompt needs the patched glyphs
# in the primary face rather than relying on a Symbols Nerd Font fallback.
font-family = "InputMono Nerd Font"
font-style = "Light"
# Matched to Terminal.app's SF Mono at 12pt, which is the reference for "normal
# size" here. Not simply 12: point size is a scale factor on the em square, and
# how big text actually looks depends on the font's proportions within it. Input
# Mono draws larger per point than SF Mono -- cap height 0.727em vs 0.705em, cell
# width 0.636em vs 0.618em -- so equal point sizes are not equal sizes on screen.
# 11.75 lands cap height and cell width within ~1% of Terminal; plain 12 is
# ~3% larger. Raise this freely, it is only a starting point that matches.
font-size = 11.75

# Loosen the line spacing. This is a delta on the font's natural line height,
# not an absolute -- 10% adds 10%. The glyph stays centred in the taller cell
# and powerline glyphs stretch with it, so the p10k prompt and tmux status line
# stay flush; the cursor does not, it tracks the font, so add
# adjust-cursor-height if it starts looking stubby. Applies to every row in the
# window uniformly, since a terminal grid has one cell height -- it is not
# something an individual program can opt into.
adjust-cell-height = 10%

# Send Ctrl-Space+p (Previous tab/window in tmux)
# See: https://arslan.io/2018/02/05/gpu-accelerated-terminal-alacritty/#make-alacritty-feel-like-iterm2
keybind = command+option+left=text:\x00\x70
# Send Ctrl-Space+n (Next tab/window in tmux)
# See: https://arslan.io/2018/02/05/gpu-accelerated-terminal-alacritty/#make-alacritty-feel-like-iterm2
keybind = command+option+right=text:\x00\x6e
