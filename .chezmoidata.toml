# Pinned versions of everything the setup scripts download by hand.
#
# These live here rather than inline so that bumping one tool is a one-line
# change in one place, and so the setup script can later be split per-tool
# without the versions being scattered across the pieces.
#
# Only tools we pin ourselves are listed. Things installed via brew/apt, or
# resolved at run time (Go reads go.dev/VERSION, Discord/Brave use "latest"
# endpoints), deliberately have no entry — they are not pinned.

[versions]

# https://github.com/neovim/neovim/releases
nvim = "0.11.4"

# Node major version, used as "v<major>" by fnm
node = "22"

# https://roamresearch.com desktop app
roam = "0.0.14"

# https://github.com/fontforge/fontforge/releases — the release tag is a plain
# date, but the asset filenames embed a dated build id, so both are needed.
fontforge = "20201107"
fontforge_build = "2020-11-07-21ad4a1"

# https://github.com/sharkdp/bat/releases
bat = "0.25.0"

# https://github.com/dandavison/delta/releases
delta = "0.18.2"

# https://github.com/LuaLS/lua-language-server/releases
lua_language_server = "3.13.9"

# https://www.mongodb.com — the apt repo and signing key are keyed on the
# release series, while the packages pin an exact patch release.
mongodb = "4.4.5"
mongodb_series = "4.4"

# https://github.com/dbeaver/dbeaver/releases
dbeaver = "22.1.0"

# https://www.pdflabs.com/tools/pdftk-the-pdf-toolkit/
pdftk = "2.02"

# https://github.com/xournalpp/xournalpp/releases
xournalpp = "1.1.1"

# https://github.com/signalapp/Signal-Desktop/releases
signal = "5.47.0"

# Playwright is used to script the PICO-8 download; pinned to a "~" range
playwright = "1.21.0"

# https://github.com/pqrs-org/Karabiner-Elements/releases
karabiner = "14.4.0"

# https://www.cockos.com/licecap/ — the download URL squashes the version into
# the filename with no separators, so 1.32 becomes "132".
licecap = "132"

# https://github.com/zsa/wally/releases
wally = "2.1.3"

# https://github.com/FiloSottile/mkcert/releases
mkcert = "1.4.4"
